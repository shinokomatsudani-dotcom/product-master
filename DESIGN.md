# 商品マスタ管理画面 — 設計メモ

## 主な利用者と目的

**在庫担当者**を主な利用者として設計した。目的は「在庫切れ」「公開状態と在庫数の食い違い（在庫切れなのに公開中のまま等）」「価格ミス（価格未設定のまま公開）」に素早く気づくこと。

## データモデル：商品（Product）とSKU（Sku）

SKUは「これ以上分解できない最小の在庫管理単位」であり、サイズ・カラー違いはそれぞれ別のSKUになる。そのため、以下の2階層でモデル化している。

- **Product（商品/型番）**: 商品名・カテゴリ・ブランド・画像・説明文など、サイズ・カラーが違っても共有される情報
- **Sku（SKU）**: SKUコード・サイズ・カラー・価格・セール価格・在庫数・公開状態・登録日・更新日など、バリエーションごとに独立して管理する情報

一覧・検索・並び替えはすべてSKU単位（1行=1SKU）で行う。これにより「在庫8、でも実はLだけ0」のような、商品単位に集計してしまうと見えなくなる異常にも気づける（詳細画面の「この商品の他のバリエーション」テーブルで実際に確認できる）。

編集フォームで商品名・カテゴリ・ブランド・画像・説明文（＝Product側の情報）を変更すると、同じ商品の他のSKUにも反映される（フォーム内に注記あり）。

制約として、既存の商品に新しいサイズ・カラーのSKUを追加するUIは今回のプロトタイプには含めていない（新規登録は常に新しい商品を1SKUとして作成する）。

## 一覧に出す項目と出さない項目

| 項目 | 一覧に出すか | 理由 |
| --- | --- | --- |
| 商品名 | 出す | 商品を識別するために必要 |
| SKU | 出す | 運用上の突合・検索に必要。SKU単位の行を一意に識別する |
| カテゴリ | 出す | 絞り込み後の一覧比較に使う |
| バリエーション（サイズ・カラー） | 出す | SKU単位の一覧では、同じ商品名の行を区別するために必須 |
| 在庫数 | 出す | 在庫担当者の最優先確認項目。0件は赤字＋アイコンで強調 |
| 公開状態 | 出す | 「売れる状態か」を一覧上で即判定するため |
| 更新日 | **出さない** | 放置検知・作業確認など二次的な用途に留まり、一覧の最優先情報ではない。詳細画面と「更新日で絞り込む」検索条件でカバーする |
| 価格 | 出さない | 価格そのものの妥当性判断は商品担当者の領域。詳細で確認する |
| セール価格 | 出さない | 価格同様、詳細確認で十分 |
| ブランド | 出さない | 比較の優先度が低い |
| 商品画像 | 出さない | 一覧に出すと表示密度が下がり比較がしづらくなる |
| 商品説明 | 出さない | 長文なので詳細画面で確認する |
| 登録日 | 出さない | 更新日と同様、一覧の主目的（比較・異常検知）には不要 |

在庫担当者にとって「公開中なのに在庫0」は見逃せない異常なので、一覧では該当行を薄いオレンジ背景＋警告アイコンで強調している（`components/product-table.tsx`）。価格は一覧には出さないが、価格ミス（0円のまま公開）は詳細画面で赤字表示して気づけるようにした。

### 更新日を列から外した理由

更新日が役立つ場面（放置検知・作業確認・新着チェック）はどれも「気づいたら見に行く」二次的な情報で、在庫担当者が画面を開いた瞬間に最優先で見たいもの（在庫切れ・非公開漏れ）とは性質が異なる。一方バリエーションは、SKU単位の一覧では無いと行の区別すらつかない必須情報のため、更新日と差し替えた。

## 検索・絞り込み条件

- 常時表示：キーワード検索（商品名・SKU）／カテゴリ／公開状態／在庫切れのみ（トグル）
  - 在庫担当者が最も頻繁に使う探し方（在庫がある商品を探す／在庫切れだけ見る／状態で絞る）に対応
- 詳細条件（折りたたみ）：価格帯（100円刻み、0円以上）／更新日の範囲
  - 使用頻度が低い条件は初期非表示にして画面をシンプルに保つ
  - 価格帯・更新日に値が入っている状態で一覧に戻ってきたときは、パネルを自動的に展開し、条件がかかっていることが見た目で分かるようにしている

条件0件時は「条件に一致する商品がありません」と表示し、「条件をリセット」で次の行動を提示する（`app/products/page.tsx`）。

## 編集フォームのグループ分け

1. **販売情報**（在庫数 → 公開状態 → 価格 → セール価格）— 在庫担当者が最も頻繁に触る情報を先頭に配置
2. **バリエーション**（SKUコード・サイズ・カラー）— このSKU固有の識別情報
3. **基本情報**（商品名・カテゴリ・ブランド）— 同じ商品の全SKUで共有される情報。編集すると他バリエーションにも反映される旨を注記
4. **表示情報**（画像URL・説明文）— 同じく商品単位で共有

価格を「販売情報」に含めたのは、価格そのものの是非ではなく「価格ミス（未設定）に気づけるか」を在庫担当者の関心事として扱うため。

## 画面構成

- `/products` — 一覧（検索・絞り込み、ローディング/空/エラー状態）
- `/products/[id]` — 詳細（この商品の他のバリエーション一覧を含む）
- `/products/[id]/edit` — 編集
- `/products/new` — 新規登録（新しい商品を1SKUとして作成）

データは localStorage に保存するプロトタイプ実装（`lib/product-store.ts`）。開発時のみ画面左下の Dev パネルからローディング/エラー状態を強制的に再現できる（本番ビルドでは非表示）。
